{
  "name": "hass-stellantis-vehicles-oauth",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.js --platform=node --bundle --format=esm --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
